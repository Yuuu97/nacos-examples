spring:
  application:
    name: nacos-example
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        # 配置中心地址
        server-addr: 127.0.0.1:8848
        namespace: public
  config:
    import:
      - nacos:application.yml
